<%= form_with(model: user_feed_subscription) do |form| %>
  <% if user_feed_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_feed_subscription.errors.count, "error") %> prohibited this user_feed_subscription from being saved:</h2>

      <ul>
        <% user_feed_subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
