<%= form_with(model: user_feed) do |form| %>
  <% if user_feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_feed.errors.count, "error") %> prohibited this user_feed from being saved:</h2>

      <ul>
        <% user_feed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
