version: '3'

services:
  tsung:
    build: .
    ports:
    - "8091:8091"
    command: tsung start
    volumes:
      - ./simple.xml:/root/.tsung/tsung.xml:ro
      - ./logs/:/root/.tsung/log
    network_mode: host

